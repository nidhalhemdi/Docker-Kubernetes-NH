# **macOS Setup**

---

# ✅ **Goal**

Set up a **local Kubernetes cluster** on macOS using:

1. **kubectl** (CLI tool)
2. **Minikube** (local single-node Kubernetes cluster)
3. **VirtualBox** (hypervisor Minikube will use)

---

# 🧩 **1. Check System Requirements**

Run this command in the terminal.

```sh
sysctl -a | grep -E --color 'machdep.cpu.features|VMX'
```

You must see **VMX** highlighted → this confirms virtualization support.

If Docker works for you, this will work too.

---

# 🛠️ **2. Install kubectl (using Homebrew)**

If you don’t have Homebrew:

```sh
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

Install kubectl:

```sh
brew install kubectl
```

Verify installation:

```sh
kubectl version --client
```

---

# 🧩 **3. Install a Hypervisor (VirtualBox recommended)**

Download VirtualBox for macOS from the VirtualBox website → **OS X hosts**.

Install it normally.
If macOS blocks the installation:

* Go to **System Settings → Privacy & Security → Allow**
* Then rerun installer.

---

# 🚀 **4. Install Minikube**

Use Homebrew again:

```sh
brew install minikube
```

---

# ▶️ **5. Start Your Local Kubernetes Cluster**

Start Minikube using the hypervisor you installed:

```sh
minikube start --driver=virtualbox
```

This:

* Creates a **VM**
* Installs **master + worker node software**
* Sets up a **single-node Kubernetes cluster**

---

# 🔍 **6. Verify the Cluster**

Check status:

```sh
minikube status
```

---

# 📊 **7. (Optional) Open Kubernetes Dashboard**

```sh
minikube dashboard
```

This opens a browser UI showing:

* Pods
* Deployments
* Cluster info

(Stop the dashboard with **Ctrl + C**.)

---

# 🎯 **Outcome**

You now have:

* `kubectl` installed (to communicate with Kubernetes)
* Minikube running a **local, single-node cluster**
* A working Kubernetes dashboard

→ Ready to deploy real Kubernetes objects.
